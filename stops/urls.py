"""
URL configuration for the stops application.

This module currently contains placeholder routes.  In future phases,
views will be added to manage locations and trip stops directly via
the web interface.
"""

from django.urls import path

app_name = "stops"

urlpatterns: list = []
