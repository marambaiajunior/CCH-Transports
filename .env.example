# Example environment configuration for CCH Logistics TMS

SECRET_KEY=your-secret-key
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
TIME_ZONE=America/Denver

# PostgreSQL database configuration (used when DEBUG=False)
# If DATABASE_URL is set, it takes precedence over POSTGRES_*.
# Example: postgres://user:password@host:5432/cchlogistics?sslmode=require
DATABASE_URL=
POSTGRES_DB=cchlogistics
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# CSRF trusted origins (comma separated)
CSRF_TRUSTED_ORIGINS=https://tms.example.com

# Email configuration (optional)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=username
EMAIL_HOST_PASSWORD=password
DEFAULT_FROM_EMAIL=info@example.com
